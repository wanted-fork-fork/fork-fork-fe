import { style } from '@repo/fork-fork-lib/configs';

export const body = style({
  maxWidth: '480px',
  height: 'calc(100vh - 44px)',
  margin: '0 auto',
  paddingTop: '44px',
  boxShadow: '0px 0px 17px 2px #0000001A',
  overflow: 'hidden',
});
